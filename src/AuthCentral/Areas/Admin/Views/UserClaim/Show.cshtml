@using Fsw.Enterprise.AuthCentral.Areas.Admin.Models
@model ClaimModelContainer

@{
    Layout = "~/Areas/Admin/Views/Shared/_UserAdminLayout.cshtml";
    ViewBag.Title = "View User Claim";
}

<div class="row">
    <div class="col-md-12" >
        <a class="btn btn-success" href="/Admin/UserClaim/Create/@Model.ClaimantId">Create Claim</a>
    </div>
</div>

@foreach(var userClaim in Model.Claims)
{
    <hr />
    @using (Html.BeginForm("Delete", "UserClaim", new { claimantId = Model.ClaimantId, area = "Admin" }, FormMethod.Post, new { @class = "form-horizontal" }))
    {
        @Html.AntiForgeryToken()
        @Html.HiddenFor(model => model.ClaimantId)
        @Html.DisplayFor(model => userClaim)
        <div class="form-group">
           <div class="col-md-offset-2 col-md-10">
               <input type="submit" value="Delete" class="btn btn-warning" />
           </div>
        </div>
    }
}

<hr />